---
interface Props {
	block?: boolean;
	title?: string;
}

const { title, block } = Astro.props;
---

{
	block ? (
		<div class="block-note">
			<span class="block-title">{title}</span>
			<slot />
		</div>
	) : (
		<blockquote>
			<slot />
		</blockquote>
	)
}

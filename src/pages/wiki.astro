---
import WikiLayout from "$layouts/WikiLayout.astro";
import { getEntryBySlug } from "astro:content";

const wikiIndex = await getEntryBySlug("wiki", "index");
---

<WikiLayout wikiItem={wikiIndex} />
